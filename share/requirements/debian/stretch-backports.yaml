# names must exist as binary packages in the suite
# for this distribution at a version which matches the constraint.
# set backports: True if the -t option is required.

# note: this will produce TWO lines of output in 'names' mode

lava-server:
  requests:
   version: ">=2.20.0"
   name: python3-requests
  django-auth-ldap:
    version: ">=1.2.12"
    name: python3-django-auth-ldap
  django-tables2:
    version: ">=1.14.2"
    name: python3-django-tables2

lava-dispatcher:
  requests:
   version: ">=2.20.0"
   name: python3-requests

lava-common:
