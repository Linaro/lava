{% extends 'base.jinja2' %}
{% block body %}
device_type: nrf52-nitrogen
actions:
  boot:
    connections:
      serial:
      ssh:
    methods:
      pyocd:
        parameters:
          command:
            pyocd-flashtool
          options:
          - -d {{ debug|default('debug') }}
          - -t {{ target|default('nrf52') }}
          - -f {{ frequency|default('3000000') }}
{% endblock -%}
