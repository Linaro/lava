# Automatically added by dh_installinit
if [ -x "/etc/init.d/lava-server" ]; then
	update-rc.d lava-server defaults >/dev/null
	invoke-rc.d lava-server start || exit $?
fi
# End automatically added section
