Image Reports are replaced by Charts
####################################

The pipeline V2 model uses :ref:`result_queries` instead of image
reports.

.. seealso:: [ `V1 section on image reports <../v1/lava-dashboard-image-reports.html>`_]
